# Java 刷题记录 - 2026年3月31日

> 作者：秋霁
> 学习日期：2026年3月31日

---

## 📚 题库目录

> 💡 [点击这里查看测试代码](TestCode2026-03-31/README.md) - 所有题目的完整可运行代码

| 题号 | 题目 | 测试代码 |
|------|------|---------|
| 1 | [什么是Java中的类](#1-什么是java中的类) | [Question01_Class.java](TestCode2026-03-31/Question01_Class.java) |
| 2 | [什么是实例化](#2-什么是实例化) | [Question02_Instantiation.java](TestCode2026-03-31/Question02_Instantiation.java) |
| 3 | [static关键字的作用](#3-static关键字的作用) | [Question03_Static.java](TestCode2026-03-31/Question03_Static.java) |
| 4 | [接口和抽象类有什么区别](#4-接口和抽象类有什么区别) | [Question04_InterfaceAbstract.java](TestCode2026-03-31/Question04_InterfaceAbstract.java) |
| 5 | [JDK和JRE有什么区别](#5-jdk和jre有什么区别) | [Question05_JDKJRE.java](TestCode2026-03-31/Question05_JDKJRE.java) |
| 6 | [你使用哪些JDK提供的工具](#6-你使用哪些jdk提供的工具) | [Question06_JDKTools.java](TestCode2026-03-31/Question06_JDKTools.java) |
| 7 | [关于Java的反射机制，如何应用反射](#7-关于java的反射机制如何应用反射) | [Question07_Reflection.java](TestCode2026-03-31/Question07_Reflection.java) |
| 8 | [如果一个线程在Java中被两次调用start()方法，会发生什么](#8-如果一个线程在java中被两次调用start方法会发生什么) | [Question08_ThreadStart.java](TestCode2026-03-31/Question08_ThreadStart.java) |
| 9 | [Java的Optional类是什么，它有什么用](#9-java的optional类是什么它有什么用) | [Question09_Optional.java](TestCode2026-03-31/Question09_Optional.java) |
| 10 | [Java中的I/O流是什么](#10-java中的io流是什么) | [Question10_IOStream.java](TestCode2026-03-31/Question10_IOStream.java) |
| 11 | [Java中的基本数据类型有哪些](#11-java中的基本数据类型有哪些) | [Question11_PrimitiveTypes.java](TestCode2026-03-31/Question11_PrimitiveTypes.java) |
| 12 | [什么是Java中的自动拆箱和装箱](#12-什么是java中的自动拆箱和装箱) | [Question12_AutoBoxing.java](TestCode2026-03-31/Question12_AutoBoxing.java) |

---

## 1. 什么是Java中的类

### 📌 回答重点

**类是对象的模板/蓝图**，定义了对象应该有什么属性（字段）和行为（方法）。

**核心概念：**
- 类是抽象的概念，不是具体的东西
- 类定义了"这类对象应该长什么样"
- 类包含：属性（字段）+ 行为（方法）

---

### 💡 面试官最爱问的3个细节

#### 1) 类的基本结构

```java
public class Student {
    // 属性（字段）
    String name;
    int age;
    
    // 行为（方法）
    public void study() {
        System.out.println(name + "正在学习");
    }
}
```

#### 2) 类的三要素

| 要素 | 说明 | 示例 |
|------|------|------|
| 字段 | 对象的数据/属性 | `String name` |
| 方法 | 对象的行为/功能 | `void study()` |
| 构造器 | 创建对象时初始化 | `public Student()` |

#### 3) 类与对象的关系

- 类 = 模具/图纸
- 对象 = 根据模具生产的产品
- 一个类可以创建无数个对象

---

### 📝 总结

| 概念 | 说明 |
|------|------|
| 类 | 模板，定义对象的结构和行为 |
| 字段 | 对象的属性数据 |
| 方法 | 对象的行为功能 |
| 构造器 | 创建并初始化对象 |

---

## 2. 什么是实例化

### 📌 回答重点

**实例化就是根据类创建具体的对象**。类只是模板，要使用它需要创建实例（对象）。

**核心流程：**
1. 声明引用变量
2. 使用 `new` 关键字创建对象
3. 调用构造器初始化对象
4. 将对象地址赋给引用变量

---

### 💡 面试官最爱问的3个细节

#### 1) 实例化的完整过程

```java
Student student1 = new Student();  // 这就是实例化
//   ↑            ↑       ↑
// 引用变量    创建对象   调用构造器
```

#### 2) 对象与引用的区别

```java
Student s1 = new Student();  // s1是引用，new Student()才是对象
Student s2 = s1;             // s2和s1指向同一个对象
```

- **引用**：存储对象的地址（像遥控器）
- **对象**：真正存储数据的实体（像电视机）
- 一个对象可以被多个引用指向

#### 3) 实例初始化顺序

```java
public class Student {
    String name = "默认值";      // 1. 字段初始化
    static int count = 0;       // 0. 静态变量初始化（类加载时）
    
    public Student() {          // 2. 构造器执行
        name = "构造器赋值";
    }
}
```

**执行顺序：** 静态初始化 → 字段初始化 → 构造器

---

### 📝 总结

| 步骤 | 代码 | 说明 |
|------|------|------|
| 声明引用 | `Student s1` | 在栈中创建引用变量 |
| 创建对象 | `new Student()` | 在堆中创建对象 |
| 赋值 | `=` | 将对象地址赋给引用 |

---

## 3. static关键字的作用

### 📌 回答重点

**static表示"属于类本身，不属于对象实例"**。

**核心特点：**
- 被所有实例共享，只有一份
- 可以直接通过类名访问
- 随类加载而初始化，不需要创建对象

---

### 💡 面试官最爱问的3个细节

#### 1) 静态变量 vs 实例变量

```java
public class Student {
    String name;           // 实例变量：每个对象独有
    static int count = 0;  // 静态变量：所有对象共享
}
```

| 类型 | 归属 | 数量 | 访问方式 |
|------|------|------|----------|
| 实例变量 | 对象 | 每个对象一份 | `对象.变量` |
| 静态变量 | 类 | 只有一份 | `类名.变量` |

#### 2) 静态方法的特点

```java
public class Student {
    String name;
    static int count = 0;
    
    // 静态方法：可直接用类名调用
    public static void showCount() {
        System.out.println("学生总数：" + count);
        // System.out.println(name);  // 错误！不能访问实例变量
    }
    
    // 实例方法：必须用对象调用
    public void study() {
        System.out.println(name + "正在学习");
        showCount();  // 可以访问静态方法
    }
}
```

**静态方法限制：**
- ✅ 可以访问静态变量和静态方法
- ❌ 不能访问实例变量和实例方法
- ❌ 不能使用 `this` 关键字

#### 3) 实际应用示例

```java
public class Student {
    String name;
    static int count = 0;
    
    public Student() {
        count++;  // 统计创建了多少个学生
    }
    
    public static void showCount() {
        System.out.println("学生总数：" + count);
    }
}

// 使用
Student s1 = new Student();
Student s2 = new Student();
Student.showCount();  // 输出：学生总数：2
```

---

### 📝 总结

| 关键点 | 说明 |
|--------|------|
| 静态变量 | 所有对象共享一份，用 `类名.变量` 访问 |
| 静态方法 | 直接用 `类名.方法()` 调用，不能访问实例成员 |
| 静态代码块 | `static { }` 类加载时执行一次 |
| 应用场景 | 工具方法、计数器、常量 |

---

### 🎯 内存图解

```
┌─────────────────────────────────────────┐
│           Student类（方法区）             │
│  ┌─────────────────────────────────┐    │
│  │ static int count = 2（共享）     │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
         ↑                    ↑
         │ 共享                │ 共享
    ┌────┴────┐          ┌────┴────┐
    │ s1对象   │          │ s2对象   │
    │ name    │          │ name    │
    └─────────┘          └─────────┘
```

**关键：** `count` 只有一份，被所有对象共享。

---

## 4. 接口和抽象类有什么区别

### 📌 回答重点

接口和抽象类在设计动机上有所不同：

**接口（自上而下）：**
- 我们知晓某一行为，于是基于这些行为约束定义了接口
- 一些类需要有这些行为，因此实现对应的接口
- 先约定接口，再实现

**抽象类（自下而上）：**
- 我们写了很多类，发现它们之间有共性，有很多代码可以复用
- 因此将公共逻辑封装成一个抽象类，减少代码冗余
- 先有一些类，才抽象了共同父类

> 💡 实战中很多时候都是因为重构才有的抽象

---

### 💡 面试官最爱问的3个细节

#### 1) 方法实现

```java
// 接口：Java 8之前只能有抽象方法
public interface Flyable {
    void fly();  // 默认是public abstract
    
    // Java 8之后可以有default方法和静态方法
    default void defaultMethod() {
        System.out.println("接口的默认方法");
    }
    
    static void staticMethod() {
        System.out.println("接口的静态方法");
    }
}

// 抽象类：可以有抽象方法和具体方法
public abstract class Animal {
    public abstract void eat();  // 抽象方法
    
    public void sleep() {        // 具体方法
        System.out.println("动物在睡觉");
    }
}
```

#### 2) 构造函数和成员变量

| 特性 | 接口 | 抽象类 |
|------|------|--------|
| 构造函数 | ❌ 不能有 | ✅ 可以有 |
| 成员变量 | `public static final`（常量） | 可以是各种访问修饰符 |
| 初始化 | 只能有常量 | 可以有实例变量 |

```java
// 接口：成员变量默认是public static final
public interface Constants {
    int MAX_VALUE = 100;  // 等价于 public static final int MAX_VALUE = 100
}

// 抽象类：成员变量可以有多种修饰符
public abstract class Animal {
    private String name;           // 私有变量
    protected int age;             // 保护变量
    public static int count = 0;   // 静态变量
    
    public Animal(String name) {   // 构造函数
        this.name = name;
    }
}
```

#### 3) 多继承

```java
// 抽象类：只能单继承
public abstract class Animal { }
public abstract class Mammal { }
// public class Dog extends Animal, Mammal { }  // ❌ 错误！不能多继承

// 接口：可以多实现
public interface Flyable { }
public interface Swimmable { }
public class Duck implements Flyable, Swimmable { }  // ✅ 正确！可以多实现
```

---

### 📝 总结

| 区别 | 接口 | 抽象类 |
|------|------|--------|
| 设计动机 | 自上而下（先定义行为） | 自下而上（后提取共性） |
| 方法 | 默认public abstract，Java 8后可有default/static | 可以有抽象和具体方法 |
| 变量 | 只能是public static final常量 | 可以是任意访问修饰符 |
| 构造器 | ❌ 不能有 | ✅ 可以有 |
| 继承 | 可以多实现 | 只能单继承 |

---

### 🔍 拓展知识：抽象类 vs 普通类 & 三种方法详解

#### 一、抽象类 vs 普通类

```java
// 普通类：可以直接实例化
public class Animal {
    public void eat() {
        System.out.println("动物在吃");
    }
}
Animal a = new Animal();  // ✅ 可以

// 抽象类：不能直接实例化
public abstract class Animal {
    public abstract void eat();  // 抽象方法
}
Animal a = new Animal();  // ❌ 错误！不能实例化抽象类
```

**关键区别：**

| 特性 | 抽象类 | 普通类 |
|------|--------|--------|
| 实例化 | ❌ 不能直接new | ✅ 可以直接new |
| 抽象方法 | ✅ 可以有 | ❌ 不能有 |
| 被继承 | 子类必须实现所有抽象方法（除非子类也是抽象类） | 子类可以选择性重写方法 |
| 设计目的 | 作为基类，定义通用行为模板 | 具体的实现类 |

---

#### 二、抽象方法、default方法、static方法

##### 1) 抽象方法（abstract）

**定义：** 只有声明，没有实现，必须由子类重写。

```java
// 抽象类中的抽象方法
public abstract class Animal {
    public abstract void eat();  // 没有方法体
}

// 接口中的抽象方法
public interface Flyable {
    void fly();  // 默认就是public abstract
}
```

**特点：**
- ❌ 不能有方法体
- ❌ 不能是private（子类需要重写）
- ❌ 不能是static（静态方法不能被重写）
- ✅ 必须被子类实现（除非子类也是抽象类）

---

##### 2) default方法（Java 8+）

**定义：** 接口中的默认实现方法，子类可以继承或重写。

```java
public interface Flyable {
    // default方法：有默认实现
    default void fly() {
        System.out.println("默认飞行方式");
    }
}

// 实现类可以选择不重写
public class Bird implements Flyable {
    // 不写fly()也能用，继承默认实现
}

// 实现类也可以重写
public class Eagle implements Flyable {
    @Override
    public void fly() {
        System.out.println("老鹰飞得很高");
    }
}
```

**特点：**
- ✅ 有方法体
- ✅ 实现类可以不重写，使用默认实现
- ✅ 实现类可以重写
- ✅ 解决了接口演进问题（给接口添加新方法不影响已有实现类）

---

##### 3) static方法（Java 8+）

**定义：** 接口或类中的静态方法，属于类型本身。

```java
public interface Flyable {
    // 静态方法
    static void showInfo() {
        System.out.println("这是飞行接口");
    }
}

// 调用方式
Flyable.showInfo();  // 用接口名调用，不能用实现类调用

public class Utils {
    public static void log(String msg) {
        System.out.println("[LOG] " + msg);
    }
}
Utils.log("测试");  // 普通类的静态方法
```

**特点：**
- ✅ 有方法体
- ❌ 不能被重写（static方法不支持多态）
- ❌ 实现类不能继承接口的static方法
- ✅ 只能用`接口名.方法名()`或`类名.方法名()`调用

---

##### 4) 三者对比表

| 特性 | 抽象方法 | default方法 | static方法 |
|------|----------|-------------|------------|
| 有无方法体 | ❌ 无 | ✅ 有 | ✅ 有 |
| 能否被重写 | ✅ 必须重写 | ✅ 可选重写 | ❌ 不能 |
| 调用方式 | 通过对象调用 | 通过对象调用 | 通过类名/接口名调用 |
| 出现位置 | 抽象类、接口 | 接口（Java 8+） | 类、接口（Java 8+） |
| 多态支持 | ✅ 支持 | ✅ 支持 | ❌ 不支持 |

---

##### 5) 实际应用场景

```java
// 接口定义
public interface Database {
    // 抽象方法：子类必须实现
    void connect();
    
    // default方法：提供默认实现，子类可选重写
    default void log(String msg) {
        System.out.println("[DB] " + msg);
    }
    
    // static方法：工具方法，不属于某个实例
    static String getVersion() {
        return "1.0.0";
    }
}

// 实现类
public class MySQL implements Database {
    @Override
    public void connect() {
        System.out.println("连接MySQL");
    }
    
    // log()可以不重写，使用默认实现
}

// 使用
MySQL db = new MySQL();
db.connect();              // 调用抽象方法
db.log("查询数据");         // 调用default方法
Database.getVersion();      // 调用static方法
```

---

### 🔗 扩展阅读

> 📖 [为什么Java不支持多重继承？](../Java2026-03-30.md#3-为什么java不支持多重继承)

---

## 5. JDK和JRE有什么区别

### 📌 回答重点

**JRE（Java Runtime Environment）**：Java运行环境，只能跑Java程序

**JDK（Java Development Kit）**：Java开发工具包，既能跑程序，又能写程序、编译程序

简单讲：**JDK = JRE + 开发工具**

```
┌─────────────────────────────────────────┐
│              JDK（开发工具包）             │
│  ┌───────────────────────────────────┐  │
│  │         JRE（运行环境）             │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │      JVM（虚拟机）           │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │   核心类库（java.lang等）    │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │   开发工具（javac、jdb、jar等）    │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

### 💡 面试官最爱问的3个细节

#### 1) JRE里面有什么

```java
JRE（Java Runtime Environment）
├── JVM（Java Virtual Machine）
│   └── 负责执行字节码，实现跨平台
├── 核心类库
│   ├── java.lang（String、Object等）
│   ├── java.util（集合框架）
│   ├── java.io（输入输出）
│   └── java.net（网络编程）
└── 配置文件和本地库
```

**JVM的作用：**
- 执行 `.class` 字节码文件
- 内存管理（堆、栈、方法区）
- 垃圾回收
- 实现跨平台（一次编写，到处运行）

---

#### 2) JDK比JRE多了什么

```java
JDK（Java Development Kit）
├── JRE（包含上面的所有内容）
└── 开发工具
    ├── javac    // 编译器：.java → .class
    ├── java      // 运行器：执行.class
    ├── jdb       // 调试器：排查问题
    ├── jar       // 打包工具：打包class文件
    ├── javadoc   // 文档生成器
    ├── jps       // 查看Java进程
    ├── jstack    // 查看线程栈
    └── jmap      // 查看内存映射
```

---

#### 3) 实际应用场景

| 场景 | 需要安装 | 原因 |
|------|----------|------|
| 运行Java程序 | JRE即可 | 只需要JVM执行字节码 |
| 开发Java程序 | 必须JDK | 需要javac编译 |
| 服务器部署 | JRE即可 | 只运行不开发 |
| 学习Java | 建议JDK | 方便练习编译运行 |

**验证安装：**

```bash
# 查看Java版本（JRE或JDK都有）
java -version

# 查看编译器版本（只有JDK有）
javac -version
```

---

### 📝 总结

| 对比项 | JRE | JDK |
|--------|-----|-----|
| 全称 | Java Runtime Environment | Java Development Kit |
| 用途 | 运行Java程序 | 开发+运行Java程序 |
| 包含 | JVM + 核心类库 | JRE + 开发工具 |
| 大小 | 较小 | 较大 |
| 适用人群 | 普通用户、运维 | 开发者 |

---

### 🔍 拓展知识：JVM、JRE、JDK的关系

```
┌──────────────────────────────────────────────────────┐
│                    JDK（完整开发环境）                   │
│  ┌────────────────────────────────────────────────┐  │
│  │                JRE（运行环境）                   │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │              JVM（虚拟机）                 │  │  │
│  │  │  - 类加载器                               │  │  │
│  │  │  - 运行时数据区（堆、栈、方法区）           │  │  │
│  │  │  - 执行引擎                               │  │  │
│  │  │  - 本地库接口                             │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │         核心类库（Java API）              │  │  │
│  │  │  - java.lang, java.util, java.io...      │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │              开发工具（Development Tools）       │  │
│  │  - javac（编译器）                              │  │
│  │  - jdb（调试器）                                │  │
│  │  - jar（打包工具）                              │  │
│  │  - javadoc（文档生成）                          │  │
│  │  - jconsole（监控工具）                         │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

**一次编写，到处运行的原理：**

```
源代码(.java)
    ↓ javac编译
字节码(.class)
    ↓ JVM执行
    ├── Windows JVM → Windows机器码
    ├── Linux JVM   → Linux机器码
    └── macOS JVM   → macOS机器码
```

---

## 6. 你使用哪些JDK提供的工具

### 📌 回答重点

JDK自带的工具可以分成三类：**开发编译类**、**监控诊断类**、**性能分析类**。

---

### 💡 面试官最爱问的3类工具

#### 1) 开发编译类

```bash
# 编译器：.java → .class
javac HelloWorld.java

# 运行器：执行字节码
java HelloWorld

# 打包工具：把多个class文件压成JAR包
jar cvf app.jar *.class

# 文档生成器：从源码注释生成API文档
javadoc -d doc *.java

# 调试器：命令行调试，可设断点、单步执行
jdb MyClass
```

| 工具 | 作用 | 示例 |
|------|------|------|
| javac | 编译器，编译源码 | `javac Main.java` |
| java | 运行器，执行字节码 | `java Main` |
| jar | 打包工具 | `jar cvf app.jar *.class` |
| javadoc | 生成API文档 | `javadoc *.java` |
| jdb | 命令行调试器 | `jdb Main` |

---

#### 2) 监控诊断类

```bash
# 列出所有Java进程的pid和主类名
jps

# 查看和动态修改JVM参数
jinfo <pid>

# 实时查看GC情况、堆内存占用、类加载数量
jstat -gc <pid> 1000  # 每秒打印一次GC情况

# 导出线程栈，排查死锁、线程阻塞问题
jstack <pid> > thread_dump.txt

# 导出堆快照或查看堆内存分布
jmap -histo <pid>         # 查看堆内存对象统计
jmap -dump:format=b,file=heap.hprof <pid>  # 导出堆快照
```

**实际案例：**

```bash
# 案例1：排查CPU 100%问题
jps                     # 找到Java进程pid
jstack <pid>            # 导出线程栈，找到占用CPU的线程

# 案例2：排查内存溢出
jmap -histo <pid>       # 查看对象数量，找到占用内存最多的对象
jmap -dump <pid>        # 导出堆快照用MAT分析

# 案例3：查看GC情况
jstat -gcutil <pid> 1000 5  # 每秒打印GC统计，共5次
```

---

#### 3) 性能分析类

```bash
# 图形化监控工具：看内存、线程、CPU曲线
jconsole

# 功能更强的图形化工具：CPU采样、内存采样、线程分析
jvisualvm
```

**jconsole功能：**
- 内存使用情况（堆、非堆）
- 线程数量和状态
- CPU使用率
- 类加载数量

**jvisualvm功能：**
- ✅ 实时监控
- ✅ CPU采样分析
- ✅ 内存采样分析
- ✅ 线程分析
- ✅ 堆转储分析
- ✅ 插件扩展

---

### 📝 总结

| 分类 | 工具 | 主要用途 |
|------|------|----------|
| 开发编译 | javac, java, jar, javadoc, jdb | 编译、运行、打包、文档、调试 |
| 监控诊断 | jps, jinfo, jstat, jstack, jmap | 进程管理、参数查看、GC监控、线程分析、内存分析 |
| 性能分析 | jconsole, jvisualvm | 图形化监控、CPU/内存采样 |

---

### 🔍 拓展知识：实际排查问题流程

#### 案例1：CPU飙高排查

```bash
1. jps                    # 找到Java进程pid
2. top -H -p <pid>        # 找到占用CPU最高的线程
3. printf "%x" <tid>      # 线程id转16进制
4. jstack <pid> | grep <hex-tid>  # 在线程栈中找到对应线程
5. 分析代码逻辑，定位问题
```

#### 案例2：内存溢出排查

```bash
1. jstat -gcutil <pid>    # 先看GC情况
2. jmap -histo <pid>      # 查看对象数量统计
3. jmap -dump <pid>       # 导出堆快照
4. 用MAT或jvisualvm分析快照
5. 找到占用内存最大的对象，分析原因
```

#### 案例3：死锁排查

```bash
1. jstack <pid>           # 导出线程栈
2. 查找 "Found one Java-level deadlock"
3. 分析死锁原因，修改代码
```

---

## 7. 关于Java的反射机制，如何应用反射

### 📌 回答重点

反射机制就是让程序在运行时能够"照镜子"看自己，动态获取类的结构信息，包括方法、字段、构造函数，还能直接操作它们。

**核心特点：**
- 编译期不需要知道具体类型
- 运行时再决定要用哪个类、调哪个方法
- 打破了封装性，可以访问private成员

**反射的核心流程：**
1. 获取Class对象：通过类名字符串、.class语法或对象实例获取
2. 获取成员信息：从Class对象中获取Field、Method、Constructor
3. 操作目标对象：创建实例、读写字段、调用方法

---

### 💡 面试官最爱问的3个细节

#### 1) 三种获取Class对象的方式

```java
// 方式1：通过类名.class（最安全，编译时检查）
Class<Student> clazz1 = Student.class;

// 方式2：通过对象实例.getClass()
Student student = new Student();
Class<?> clazz2 = student.getClass();

// 方式3：通过Class.forName()（最灵活，运行时动态加载）
Class<?> clazz3 = Class.forName("com.example.Student");

// 三种方式获取的是同一个Class对象
System.out.println(clazz1 == clazz2);  // true
System.out.println(clazz2 == clazz3);  // true
```

**对比：**

| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 类名.class | 编译时检查，安全 | 需要知道具体类名 | 已知类名时 |
| 对象.getClass() | 不需要import | 需要先有对象实例 | 有对象时 |
| Class.forName() | 最灵活，可动态加载 | 需要处理异常 | 配置文件、框架 |

---

#### 2) 获得Class对象后可以做的事情

```java
// 1. 获取类的基本信息
Class<?> clazz = Student.class;

String className = clazz.getName();          // 全限定名：com.example.Student
String simpleName = clazz.getSimpleName();   // 简单名称：Student

// 2. 获取构造函数并创建实例
Constructor<?> constructor = clazz.getConstructor(String.class, int.class);
Object instance = constructor.newInstance("张三", 20);

// 3. 获取字段并操作
Field nameField = clazz.getDeclaredField("name");
nameField.setAccessible(true);  // 突破private限制
nameField.set(instance, "李四");
Object value = nameField.get(instance);

// 4. 获取方法并调用
Method studyMethod = clazz.getMethod("study", String.class);
studyMethod.invoke(instance, "数学");  // 调用实例的study方法
```

---

#### 3) 实际应用场景

```java
// 场景1：框架的依赖注入（Spring）
String className = "com.example.UserService";  // 从配置文件读取
Class<?> clazz = Class.forName(className);
Object service = clazz.newInstance();  // 动态创建实例

// 场景2：动态代理
interface UserService {
    void save();
}
UserService proxy = (UserService) Proxy.newProxyInstance(
    clazz.getClassLoader(),
    new Class[]{UserService.class},
    (obj, method, args) -> {
        System.out.println("执行前...");
        Object result = method.invoke(target, args);
        System.out.println("执行后...");
        return result;
    }
);

// 场景3：JSON序列化/反序列化（Gson、Jackson）
// 反射获取所有字段，遍历赋值
for (Field field : clazz.getDeclaredFields()) {
    field.setAccessible(true);
    field.set(obj, value);
}
```

---

### 📝 总结

| 类/方法 | 作用 |
|---------|------|
| `Class.forName()` | 动态加载类 |
| `clazz.getConstructor()` | 获取构造函数 |
| `clazz.getMethod()` | 获取方法 |
| `clazz.getField()` | 获取字段 |
| `constructor.newInstance()` | 创建实例 |
| `method.invoke()` | 调用方法 |
| `field.set()/get()` | 读写字段 |
| `setAccessible(true)` | 突破private限制 |

---

### 🔍 拓展知识：反射的优缺点

**优点：**
- ✅ 灵活性高，可以在运行时动态操作
- ✅ 降低代码耦合度
- ✅ 框架必备（Spring、MyBatis、Hibernate）

**缺点：**
- ❌ 性能较低（比直接调用慢很多）
- ❌ 破坏封装性，不安全
- ❌ 代码可读性差
- ❌ 编译期无法检查错误

**性能优化：**
```java
// 使用缓存
private static final Map<String, Method> METHOD_CACHE = new HashMap<>();

public Method getMethod(String name) {
    return METHOD_CACHE.computeIfAbsent(name, 
        key -> clazz.getMethod(key));
}
```

---

### ⚠️ 注意事项

1. **安全性问题**
```java
Field field = clazz.getDeclaredField("password");
field.setAccessible(true);  // 可以访问private字段
// 这会破坏封装，慎用！
```

2. **性能问题**
```java
// 避免频繁反射，可以用缓存
private static final Method METHOD;

static {
    try {
        METHOD = MyClass.class.getMethod("myMethod");
    } catch (NoSuchMethodException e) {
        throw new RuntimeException(e);
    }
}
```

3. **异常处理**
```java
try {
    Class<?> clazz = Class.forName("com.example.Student");
    Object instance = clazz.newInstance();
} catch (ClassNotFoundException e) {
    // 类不存在
} catch (InstantiationException | IllegalAccessException e) {
    // 实例化失败
}
```

---

## 8. 如果一个线程在Java中被两次调用start()方法，会发生什么

### 📌 回答重点

直接抛 `IllegalThreadStateException`。

Java规定一个Thread对象只能调用一次 `start()`，第二次调用时线程状态已经不是 `NEW` 了，JVM会拒绝启动并抛异常。

**线程状态是单向流转的，一旦离开NEW状态就回不去了。** 这跟操作系统线程的设计有关：一个线程执行完毕后，它的栈空间、寄存器上下文都被回收了，没法复活。

---

### 💡 面试官最爱问的3个细节

#### 1) start()的源码分析

```java
// JDK Thread类的start()源码
public synchronized void start() {
    // 检查线程状态
    if (threadStatus != 0)
        throw new IllegalThreadStateException();

    // 加入线程组
    group.add(this);

    boolean started = false;
    try {
        start0();  // 调用native方法启动线程
        started = true;
    } finally {
        try {
            if (!started) {
                group.threadStartFailed(this);
            }
        } catch (Throwable ignore) {
            // 忽略
        }
    }
}

// native方法
private native void start0();
```

**关键点：**
- `threadStatus != 0` 检查线程状态
- `NEW` 状态下 `threadStatus = 0`
- 一旦启动，`threadStatus` 就变成非0值
- 再次调用时直接抛异常

---

#### 2) 错误演示

```java
Thread thread = new Thread(() -> {
    System.out.println("线程执行中...");
});

thread.start();  // ✅ 正常启动

// 等待线程结束
try {
    thread.join();
} catch (InterruptedException e) {
    e.printStackTrace();
}

thread.start();  // ❌ 抛出 IllegalThreadStateException
```

**输出：**
```
线程执行中...
Exception in thread "main" java.lang.IllegalThreadStateException
    at java.lang.Thread.start(Thread.java:708)
```

---

#### 3) 如何复用线程

既然不能重复启动，那如何复用？

```java
// 方案1：每次创建新线程
for (int i = 0; i < 5; i++) {
    Thread thread = new Thread(() -> {
        System.out.println("任务执行");
    });
    thread.start();
}

// 方案2：使用线程池（推荐）
ExecutorService executor = Executors.newFixedThreadPool(3);
for (int i = 0; i < 5; i++) {
    executor.submit(() -> {
        System.out.println("任务执行");
    });
}
executor.shutdown();

// 方案3：使用Runnable，创建新Thread
Runnable task = () -> System.out.println("任务执行");
for (int i = 0; i < 5; i++) {
    new Thread(task).start();
}
```

---

### 📝 总结

| 问题 | 答案 |
|------|------|
| 两次调用start() | 抛出 IllegalThreadStateException |
| 原因 | 线程状态单向流转，NEW状态只能用一次 |
| 如何复用 | 线程池或每次创建新线程 |
| 最佳实践 | 使用线程池管理线程 |

---

### 🔍 拓展知识：Java线程的六种状态

#### 状态流转图

```
        ┌─────────────────────────────────────────┐
        │                                         │
        ▼                                         │
    ┌──────┐  start()  ┌─────────┐                │
    │ NEW  │ ────────> │ RUNNABLE│◄───────────────┤
    └──────┘           └─────────┘                │
                          │   ▲                    │
                          │   │                    │
            ┌─────────────┘   │                    │
            │                 │                    │
            ▼                 │                    │
    ┌─────────────┐           │                    │
    │  BLOCKED    │───────────┘                    │
    │ (等待锁)     │                                │
    └─────────────┘                                │
                                                   │
    ┌─────────────┐           ┌──────────────┐    │
    │  WAITING    │<──────────│              │    │
    │ (无限等待)   │──────────>│  RUNNABLE    │────┘
    └─────────────┘           └──────────────┘
                                ▲      │
    ┌─────────────────┐        │      │
    │ TIMED_WAITING   │────────┘      │
    │ (计时等待)       │               │
    └─────────────────┘               │
                                      │
                                ┌──────────┐
                                │TERMINATED│
                                │  (终止)   │
                                └──────────┘
```

#### 六种状态详解

```java
public enum State {
    NEW,              // 新建：线程刚创建，还没调用start()
    RUNNABLE,         // 可运行：正在运行或等待CPU时间片
    BLOCKED,          // 阻塞：等待获取锁（synchronized）
    WAITING,          // 等待：无限期等待（wait、join、park）
    TIMED_WAITING,    // 计时等待：有限期等待（sleep、wait、join）
    TERMINATED;       // 终止：线程执行完毕
}
```

#### 各状态示例

```java
// NEW -> RUNNABLE
Thread thread = new Thread(() -> {});  // NEW
thread.start();  // RUNNABLE

// RUNNABLE -> BLOCKED
synchronized(lock) {
    // 线程1持有锁，线程2等待锁 -> BLOCKED
}

// RUNNABLE -> WAITING
Object.wait();      // 等待notify()
Thread.join();      // 等待线程结束
LockSupport.park(); // 等待unpark()

// RUNNABLE -> TIMED_WAITING
Thread.sleep(1000);     // 睡眠1秒
Object.wait(1000);      // 等待1秒
Thread.join(1000);      // 等待线程1秒
LockSupport.parkNanos(1000000); // 等待指定纳秒

// RUNNABLE -> TERMINATED
// 线程执行完run()方法，正常结束或异常退出
```

#### 状态查询

```java
Thread thread = new Thread(() -> {
    try {
        Thread.sleep(1000);
    } catch (InterruptedException e) {
        e.printStackTrace();
    }
});

System.out.println(thread.getState());  // NEW
thread.start();
System.out.println(thread.getState());  // RUNNABLE

Thread.sleep(100);
System.out.println(thread.getState());  // TIMED_WAITING

Thread.sleep(2000);
System.out.println(thread.getState());  // TERMINATED
```

---

## 9. Java的Optional类是什么，它有什么用

### 📌 回答重点

Optional是Java 8引入的一个容器类，专门用来包装可能为空的值。

**核心作用：**
- 不是消灭空指针，而是让代码更清晰地表达"这个值可能不存在"的语义
- 提供一套链式API简化空值处理

**设计理念：**

Java语言架构师Brian Goetz说得很明白：**Optional是给方法返回值用的，用来表示"没有结果"，而不是返回null。**

---

### 💡 面试官最爱问的对比

#### 对比：传统方式 vs Optional方式

**场景：查找用户，获取用户名称长度**

```java
// ❌ 传统方式：返回null，调用方容易忘记判空
public User findUser(String id) {
    return userDao.findById(id);  // 可能返回null
}

// 调用方（容易忘记判空）
User user = findUser("123");
// 如果user为null，下一行抛NullPointerException
System.out.println(user.getName().length());
```

```java
// ✅ Optional方式：明确表示"可能没有"
public Optional<User> findUser(String id) {
    User user = userDao.findById(id);
    return Optional.ofNullable(user);  // 包装成Optional
}

// 调用方（必须处理空值情况）
Optional<User> userOpt = findUser("123");
String name = userOpt
    .map(User::getName)          // 有值则映射
    .orElse("默认名称");           // 没值则返回默认值
System.out.println(name);
```

---

### 💡 面试官最爱问的3个细节

#### 1) Optional的创建方式

```java
// 创建空的Optional
Optional<User> empty = Optional.empty();

// 创建非空Optional（如果传入null会抛NPE）
Optional<User> opt1 = Optional.of(user);

// 创建可能为空的Optional（推荐）
Optional<User> opt2 = Optional.ofNullable(user);  // user可以为null
```

| 方法 | 参数为null时 | 使用场景 |
|------|-------------|----------|
| `empty()` | - | 明确返回空 |
| `of()` | 抛出NullPointerException | 确定不为null |
| `ofNullable()` | 返回`Optional.empty()` | 可能为null |

---

#### 2) Optional的常用API

```java
Optional<String> opt = Optional.ofNullable(getValue());

// 获取值
String value1 = opt.get();                    // 不推荐，可能抛异常
String value2 = opt.orElse("默认值");          // 推荐：没值返回默认值
String value3 = opt.orElseGet(() -> "默认值"); // 推荐：懒加载默认值
String value4 = opt.orElseThrow(() -> new RuntimeException("找不到")); // 抛出自定义异常

// 判断和处理
if (opt.isPresent()) {                        // 判断是否有值
    System.out.println(opt.get());
}

opt.ifPresent(val -> System.out.println(val)); // 推荐：有值才执行

// 转换和过滤
opt.map(val -> val.toUpperCase())            // 转换
   .filter(val -> val.length() > 3)          // 过滤
   .orElse("默认值");

// 链式调用
opt.map(User::getName)
   .map(String::toUpperCase)
   .orElse("UNKNOWN");
```

---

#### 3) 实际应用示例

**示例1：查询用户并处理**

```java
public Optional<User> findById(String id) {
    return Optional.ofNullable(userDao.findById(id));
}

// 使用
String name = findById("123")
    .map(User::getName)
    .orElse("未找到用户");

// 链式查询
String city = findById("123")
    .map(User::getAddress)
    .map(Address::getCity)
    .orElse("未知城市");
```

**示例2：Optional作为方法参数（不推荐）**

```java
// ❌ 不推荐：Optional作为参数
public void updateUser(Optional<User> user) {
    // 不好的设计
}

// ✅ 推荐：方法重载或默认参数
public void updateUser(User user) {
    // ...
}

public void updateUser(User user, boolean allowEmpty) {
    // ...
}
```

---

### 📝 总结

| 方法 | 作用 | 推荐指数 |
|------|------|----------|
| `get()` | 获取值（可能抛异常） | ⭐ 不推荐 |
| `orElse(default)` | 没值返回默认值 | ⭐⭐⭐ 推荐 |
| `orElseGet(supplier)` | 懒加载默认值 | ⭐⭐⭐⭐ 强烈推荐 |
| `orElseThrow(exception)` | 没值抛异常 | ⭐⭐⭐ 推荐 |
| `ifPresent(consumer)` | 有值才执行 | ⭐⭐⭐⭐ 强烈推荐 |
| `map(function)` | 转换值 | ⭐⭐⭐⭐ 强烈推荐 |
| `filter(predicate)` | 过滤值 | ⭐⭐⭐ 推荐 |

---

### 🔍 拓展知识：Optional的最佳实践

#### ✅ 推荐用法

```java
// 1. 作为方法返回值
public Optional<User> findById(String id) {
    return Optional.ofNullable(userDao.findById(id));
}

// 2. 链式调用
String name = findById("123")
    .map(User::getName)
    .orElse("默认名称");

// 3. 组合多个Optional
Optional<User> user1 = findById("1");
Optional<User> user2 = findById("2");

String result = user1.or(() -> user2)  // Java 9+
    .map(User::getName)
    .orElse("都没找到");
```

#### ❌ 不推荐用法

```java
// 1. 不要用于字段
public class User {
    private Optional<String> nickname;  // ❌ 不推荐
    private String nickname;            // ✅ 推荐
}

// 2. 不要用于方法参数
public void process(Optional<String> value);  // ❌ 不推荐
public void process(String value);            // ✅ 推荐

// 3. 不要直接调用get()
Optional<User> opt = findById("123");
User user = opt.get();  // ❌ 可能抛异常

// 4. 不要用Optional包装集合
Optional<List<User>> users;  // ❌ 不推荐
List<User> users;            // ✅ 推荐（空集合表示没有数据）
```

---

### ⚠️ 注意事项

1. **性能问题**：Optional创建对象有开销，不要滥用

```java
// ❌ 每次调用都创建Optional对象
public Optional<String> getName() {
    return Optional.ofNullable(this.name);
}

// ✅ 如果确定不为null，直接返回
public String getName() {
    return this.name;
}
```

2. **序列化问题**：Optional没有实现Serializable

```java
// ❌ 不能直接序列化
public class User implements Serializable {
    private Optional<String> nickname;  // 无法序列化
}
```

3. **使用场景**：主要用于方法返回值

```java
// ✅ 正确：返回可能为空的查询结果
public Optional<User> findById(String id);

// ❌ 错误：用于字段或参数
private Optional<String> name;
public void setName(Optional<String> name);
```

---

## 10. Java中的I/O流是什么

### 📌 回答重点

I/O流是Java用于处理数据输入输出的机制，是程序与外部世界（文件、网络、控制台）通信的桥梁。

**核心概念：**
- **流（Stream）**：数据的有序序列，像水管一样单向流动
- **输入流（Input）**：从外部读取数据到程序
- **输出流（Output）**：从程序写出数据到外部

**流的分类：**

| 分类维度 | 类型 | 说明 |
|---------|------|------|
| 数据单位 | 字节流 | 处理二进制数据（图片、视频、压缩包） |
|  | 字符流 | 处理文本数据（UTF-8、GBK编码的文本） |
| 流向 | 输入流 | 读取数据 |
|  | 输出流 | 写入数据 |
| 功能 | 节点流 | 直接操作数据源（文件、网络） |
|  | 处理流 | 包装其他流，增强功能（缓冲、转换） |

---

### 💡 面试官最爱问的3个细节

#### 1) 经典的文件读写实例

**使用BufferedReader配合try-with-resources自动关闭资源：**

```java
// 读取文件内容
try (BufferedReader reader = new BufferedReader(new FileReader("config.txt"))) {
    String line;
    while ((line = reader.readLine()) != null) {
        System.out.println(line);
    }
}  // 自动关闭资源，无需finally

// 写入文件内容
try (BufferedWriter writer = new BufferedWriter(new FileWriter("output.txt"))) {
    writer.write("Hello World");
    writer.newLine();
    writer.write("Java I/O");
}  // 自动关闭资源
```

**字节流示例：**

```java
// 复制文件（字节流）
try (FileInputStream fis = new FileInputStream("source.jpg");
     FileOutputStream fos = new FileOutputStream("target.jpg")) {
    
    byte[] buffer = new byte[1024];
    int len;
    while ((len = fis.read(buffer)) != -1) {
        fos.write(buffer, 0, len);
    }
}
```

---

#### 2) 底层原理

**字节流：**
- 直接跟操作系统的文件描述符打交道
- 每次read/write都可能触发系统调用
- 适合处理二进制文件（图片、视频、压缩包）

**字符流：**
- 在字节流基础上加了一层编码转换
- 内部用 `StreamEncoder`/`StreamDecoder` 把字节和字符互相转换
- 默认用平台编码，也可以手动指定UTF-8等字符集
- 适合处理文本文件

```
字符流的工作原理：

程序（字符） 
    ↓
StreamEncoder（编码：字符→字节）
    ↓
字节流（字节）
    ↓
操作系统/文件
```

---

#### 3) 常用I/O流类

**字节流：**

| 类 | 说明 |
|---|------|
| `FileInputStream` / `FileOutputStream` | 文件字节流 |
| `BufferedInputStream` / `BufferedOutputStream` | 缓冲字节流 |
| `DataInputStream` / `DataOutputStream` | 数据流（读写基本类型） |
| `ObjectInputStream` / `ObjectOutputStream` | 对象流（序列化） |

**字符流：**

| 类 | 说明 |
|---|------|
| `FileReader` / `FileWriter` | 文件字符流 |
| `BufferedReader` / `BufferedWriter` | 缓冲字符流 |
| `InputStreamReader` / `OutputStreamWriter` | 转换流（字节↔字符） |
| `PrintWriter` | 打印流（格式化输出） |

---

### 📝 总结

| 场景 | 推荐流 | 示例 |
|------|--------|------|
| 读取文本文件 | BufferedReader | `new BufferedReader(new FileReader(file))` |
| 写入文本文件 | BufferedWriter | `new BufferedWriter(new FileWriter(file))` |
| 复制二进制文件 | BufferedInputStream/OutputStream | 缓冲字节流 |
| 对象序列化 | ObjectOutputStream | `writeObject(obj)` |
| 控制台输入 | BufferedReader | `System.in` + `InputStreamReader` |

---

### 🔍 拓展知识

#### 1) I/O流的设计思想

**装饰器模式（Decorator Pattern）：**

```
new BufferedReader(new FileReader("file.txt"))
    ↑              ↑
  装饰器          被装饰对象
    
像穿衣服一样，一层层包装增强功能
```

**好处：**
- 动态扩展功能，不修改原有类
- 灵活组合，按需添加功能
- 避免类爆炸

```java
// 节点流：直接操作文件
FileReader fr = new FileReader("file.txt");

// 处理流：缓冲功能
BufferedReader br = new BufferedReader(fr);

// 还可以继续包装（不常见，但可以）
LineNumberReader lnr = new LineNumberReader(br);
```

---

#### 2) 缓冲流提速的原理

**为什么缓冲流更快？**

```java
// 无缓冲：每次read()都调用系统调用
FileInputStream fis = new FileInputStream("file.txt");
int b = fis.read();  // 系统调用

// 有缓冲：先读到内存，再从内存取
BufferedInputStream bis = new BufferedInputStream(fis);
int b = bis.read();  // 从内存缓冲区取，减少系统调用
```

**对比：**

| 方式 | 读取1MB文件 | 系统调用次数 |
|------|------------|--------------|
| 无缓冲 | ~1000次 | ~1000次 |
| 有缓冲（8KB缓冲区） | ~128次 | ~128次 |

**默认缓冲区大小：**
- `BufferedInputStream`：8192字节（8KB）
- `BufferedReader`：8192字符

**手动指定缓冲区大小：**

```java
// 16KB缓冲区
BufferedReader br = new BufferedReader(new FileReader("file.txt"), 16384);
```

---

#### 3) 字节流和字符流的选择

**选择标准：**

```
┌─────────────────────────────────────┐
│    文件类型是什么？                  │
└──────────────┬──────────────────────┘
               │
       ┌───────┴───────┐
       │               │
   文本文件         二进制文件
   (.txt/.csv      (.jpg/.mp4
    .xml/.json)     .zip/.exe)
       │               │
       ↓               ↓
   字符流           字节流
 FileReader       FileInputStream
 FileWriter       FileOutputStream
```

**示例：**

```java
// ✅ 文本文件：字符流
try (BufferedReader br = new BufferedReader(new FileReader("log.txt"))) {
    String line = br.readLine();
}

// ✅ 二进制文件：字节流
try (BufferedInputStream bis = new BufferedInputStream(
        new FileInputStream("image.jpg"))) {
    byte[] data = bis.readAllBytes();
}

// ❌ 错误：用字符流读二进制文件
FileReader fr = new FileReader("image.jpg");  // 会乱码！
```

**特殊情况：**

```java
// 字节流转字符流（指定编码）
try (BufferedReader br = new BufferedReader(
        new InputStreamReader(
            new FileInputStream("file.txt"), "UTF-8"))) {
    // ...
}
```

---

#### 4) Java NIO的出现

**传统I/O（BIO）的问题：**
- 阻塞式：一个线程只能处理一个连接
- 线程开销大：1000个连接需要1000个线程
- 效率低：大量线程处于等待状态

**NIO（Non-blocking I/O，Java 1.4）：**

```java
// 传统BIO：阻塞等待
ServerSocket server = new ServerSocket(8080);
Socket socket = server.accept();  // 阻塞等待连接
InputStream is = socket.getInputStream();  // 阻塞等待数据

// NIO：非阻塞
Selector selector = Selector.open();
ServerSocketChannel serverChannel = ServerSocketChannel.open();
serverChannel.configureBlocking(false);  // 设置非阻塞
serverChannel.register(selector, SelectionKey.OP_ACCEPT);

// 一个线程管理多个连接
while (true) {
    selector.select();  // 检查就绪的通道
    Set<SelectionKey> keys = selector.selectedKeys();
    for (SelectionKey key : keys) {
        if (key.isAcceptable()) {
            // 处理连接
        } else if (key.isReadable()) {
            // 处理数据
        }
    }
}
```

**NIO三大组件：**

| 组件 | 说明 |
|------|------|
| Channel（通道） | 双向通道，可读可写 |
| Buffer（缓冲区） | 数据容器，读写都经过缓冲区 |
| Selector（选择器） | 多路复用器，一个线程管理多个通道 |

**对比：**

| 特性 | BIO | NIO |
|------|-----|-----|
| 阻塞 | 阻塞 | 非阻塞 |
| 线程模型 | 一连接一线程 | 一线程多连接 |
| 适用场景 | 连接数少 | 连接数多（高并发） |

---

#### 5) 常见的坑

**坑1：忘记关闭流导致资源泄漏**

```java
// ❌ 错误：忘记关闭
FileInputStream fis = new FileInputStream("file.txt");
String content = readFromStream(fis);
// fis忘记关闭！

// ✅ 正确：try-with-resources自动关闭
try (FileInputStream fis = new FileInputStream("file.txt")) {
    String content = readFromStream(fis);
}
```

**坑2：字符编码问题**

```java
// ❌ 错误：使用平台默认编码
FileReader fr = new FileReader("file.txt");  // 可能乱码

// ✅ 正确：显式指定编码
InputStreamReader isr = new InputStreamReader(
    new FileInputStream("file.txt"), "UTF-8");
```

**坑3：缓冲流忘记flush**

```java
// ❌ 错误：可能丢失数据
BufferedWriter bw = new BufferedWriter(new FileWriter("file.txt"));
bw.write("Hello");
// 忘记flush或close，数据可能还在缓冲区

// ✅ 正确：手动flush或close
bw.flush();  // 强制刷新缓冲区
bw.close();  // close会自动flush
```

**坑4：文件路径问题**

```java
// ❌ 错误：硬编码路径，跨平台有问题
File file = new File("C:\\temp\\file.txt");  // Windows专用

// ✅ 正确：使用File.separator
File file = new File("temp" + File.separator + "file.txt");

// ✅ 更好：使用Path（Java 7+）
Path path = Paths.get("temp", "file.txt");
```

**坑5：读取大文件内存溢出**

```java
// ❌ 错误：一次性读取大文件
byte[] data = Files.readAllBytes(Paths.get("large.mp4"));  // OOM!

// ✅ 正确：流式读取，分块处理
try (BufferedInputStream bis = new BufferedInputStream(
        new FileInputStream("large.mp4"))) {
    byte[] buffer = new byte[8192];
    int len;
    while ((len = bis.read(buffer)) != -1) {
        // 处理一块数据
    }
}
```

---

## 11. Java中的基本数据类型有哪些

### 📌 回答重点

Java有**8种基本数据类型**，按用途分为四类：**整型**、**浮点型**、**字符型**和**布尔型**。

**核心特点：**
- 基本类型存储在栈中（值类型）
- 不需要new创建，直接存储数据值
- 不是对象，没有方法和属性
- 效率高，内存占用小

---

### 💡 面试官最爱问的细节

#### 1) 8种基本数据类型一览表

**整型（4种）：**

| 类型 | 占用字节 | 位数 | 取值范围 | 默认值 | 用途 |
|------|---------|------|---------|--------|------|
| byte | 1字节 | 8位 | -128 ~ 127 | 0 | 字节数据、节省空间 |
| short | 2字节 | 16位 | -32768 ~ 32767 | 0 | 短整数、节省空间 |
| int | 4字节 | 32位 | -2³¹ ~ 2³¹-1 | 0 | 整数（默认） |
| long | 8字节 | 64位 | -2⁶³ ~ 2⁶³-1 | 0L | 大整数、时间戳 |

**浮点型（2种）：**

| 类型 | 占用字节 | 位数 | 取值范围 | 默认值 | 用途 |
|------|---------|------|---------|--------|------|
| float | 4字节 | 32位 | ±3.4E38 | 0.0f | 单精度浮点 |
| double | 8字节 | 64位 | ±1.7E308 | 0.0d | 双精度浮点（默认） |

**字符型（1种）：**

| 类型 | 占用字节 | 位数 | 取值范围 | 默认值 | 用途 |
|------|---------|------|---------|--------|------|
| char | 2字节 | 16位 | 0 ~ 65535 | '\u0000' | Unicode字符 |

**布尔型（1种）：**

| 类型 | 占用字节 | 位数 | 取值范围 | 默认值 | 用途 |
|------|---------|------|---------|--------|------|
| boolean | 1字节（理论） | 1位（理论） | true/false | false | 逻辑判断 |

---

#### 2) 类型对比和使用场景

**整型选择：**
```java
// ✅ 推荐：一般整数用int（默认）
int age = 25;
int count = 100;

// ✅ 大整数用long（时间戳、大数值）
long timestamp = System.currentTimeMillis();
long population = 7800000000L;

// ✅ 节省空间时用byte/short
byte[] data = new byte[1024];  // 字节数组
short temperature = -10;
```

**浮点型选择：**
```java
// ✅ 推荐：默认用double（精度更高）
double price = 99.99;
double pi = 3.14159265358979;

// ✅ 内存敏感时用float（单精度）
float f = 99.99f;  // 必须加f后缀

// ❌ 注意：float必须加f后缀
// float f2 = 99.99;  // 错误！默认是double
```

**字符型：**
```java
// ✅ char：单个Unicode字符
char letter = 'A';
char chinese = '中';

// ✅ 转义字符
char tab = '\t';
char newline = '\n';
char unicode = '\u4e2d';  // '中'

// ❌ 注意：char只能存一个字符
// char c = 'AB';  // 错误！
String s = "AB";  // 正确：用String
```

**布尔型：**
```java
// ✅ boolean：只有true/false
boolean isTrue = true;
boolean isFalse = false;

// ❌ 注意：不能和0/1互换（区别于C语言）
```

---

#### 3) 类型转换

**自动类型转换（小→大）：**
```
byte → short → int → long → float → double
        ↑
       char
```

```java
int i = 100;
long l = i;      // 自动转换
double d = l;    // 自动转换
```

**强制类型转换（大→小）：**
```java
double d = 99.99;
int i = (int) d;     // 强制转换，结果：99（截断小数）

// ❌ 注意：可能溢出
int big = 300;
byte b = (byte) big; // 结果：44（溢出）
```

---

### 📝 总结

| 分类 | 类型 | 字节 | 默认值 | 典型用途 |
|------|------|------|--------|----------|
| 整型 | byte | 1 | 0 | 字节数据 |
| 整型 | short | 2 | 0 | 短整数 |
| 整型 | int | 4 | 0 | 整数（默认） |
| 整型 | long | 8 | 0L | 大整数、时间戳 |
| 浮点 | float | 4 | 0.0f | 单精度浮点 |
| 浮点 | double | 8 | 0.0d | 双精度浮点（默认） |
| 字符 | char | 2 | '\u0000' | Unicode字符 |
| 布尔 | boolean | 1 | false | 逻辑判断 |

---

### 🔍 拓展知识：常见陷阱

#### 陷阱1：整数溢出

```java
// ❌ 错误：int溢出
int max = 2147483647;
max = max + 1;  // 结果：-2147483648（溢出！）

// ✅ 正确：使用long
long bigNum = 2147483648L;

// ✅ 正确：Math.addExact检测溢出
try {
    int result = Math.addExact(max, 1);
} catch (ArithmeticException e) {
    System.out.println("溢出了！");
}
```

#### 陷阱2：浮点数精度问题

```java
// ❌ 错误：浮点数不精确
double d1 = 0.1;
double d2 = 0.2;
System.out.println(d1 + d2);  // 0.30000000000000004

// ✅ 正确：使用BigDecimal（金融计算）
BigDecimal bd1 = new BigDecimal("0.1");
BigDecimal bd2 = new BigDecimal("0.2");
BigDecimal sum = bd1.add(bd2);
System.out.println(sum);  // 0.3

// ❌ 注意：不要用double构造BigDecimal
BigDecimal bad = new BigDecimal(0.1);     // 不精确！
BigDecimal good = new BigDecimal("0.1");   // 精确
```

#### 陷阱3：char的取值范围

```java
// ❌ 错误：char是无符号的（0~65535）
// char c = -1;  // 错误！不能为负

// ✅ 正确：范围是0~65535
char c1 = 0;
char c2 = 65535;
```

#### 陷阱4：类型提升

```java
// ❌ 错误：byte运算会提升为int
byte a = 10;
byte b = 20;
// byte c = a + b;  // 错误！结果是int
byte c = (byte) (a + b);  // 正确：强制转换

// ✅ 注意：+=会自动处理类型转换
byte d = 10;
d += 20;  // 正确：自动转换为byte
```

#### 陷阱5：long字面量后缀

```java
// ❌ 错误：超过int范围不加L
// long big = 2147483648;  // 错误！超出int范围

// ✅ 正确：加L后缀
long big = 2147483648L;

// 注意：小写l容易和数字1混淆
long num = 100L;   // 推荐：大写L
// long num2 = 100l;  // 不推荐：看起来像101
```

---

### ⚠️ 最佳实践

```java
// 1. 选择合适的类型
int count = 100;                    // 一般整数：int
long timestamp = System.currentTimeMillis();  // 大数值：long
double price = 99.99;               // 小数：double
BigDecimal money = new BigDecimal("100.00");  // 精确计算：BigDecimal

// 2. 避免魔法数字
private static final int STATUS_ACTIVE = 1;
if (status == STATUS_ACTIVE) { }

// 3. 使用包装类常量
int max = Integer.MAX_VALUE;   // 2147483647
int min = Integer.MIN_VALUE;   // -2147483648
int size = Integer.BYTES;      // 4
```

---

## 12. 什么是Java中的自动拆箱和装箱

### 📌 回答重点

**自动装箱（Autoboxing）**：Java编译器自动把基本类型转成对应的包装类型，比如 `int` 变成 `Integer`。

**自动拆箱（Unboxing）**：反过来，把包装类型自动转成基本类型。

这俩机制是Java 5引入的，目的很简单：**让代码写起来更顺手**。以前往集合里塞个数字还得手动 `new Integer(10)`，现在直接塞 `10` 就行了。

---

### 💡 面试官最爱问的3个细节

#### 1) 装箱和拆箱的转换过程

```java
// 装箱过程
int i = 10;
Integer obj = i;  // 自动装箱
// 编译器实际调用：Integer.valueOf(i)

// 拆箱过程
Integer obj = Integer.valueOf(10);
int i = obj;      // 自动拆箱
// 编译器实际调用：obj.intValue()

// 集合中使用
List<Integer> list = new ArrayList<>();
list.add(10);     // 自动装箱：Integer.valueOf(10)
int num = list.get(0);  // 自动拆箱：Integer.intValue()
```

**转换流程：**

```
基本类型 int
    ↓ Integer.valueOf()
包装类型 Integer
    ↓ intValue()
基本类型 int
```

---

#### 2) 底层实现

**装箱：`Integer.valueOf(int)` 源码分析**

```java
public static Integer valueOf(int i) {
    // 缓存范围：-128 ~ 127
    if (i >= IntegerCache.low && i <= IntegerCache.high)
        return IntegerCache.cache[i + (-IntegerCache.low)];
    return new Integer(i);  // 超出缓存范围，new新对象
}

// Integer内部缓存类
private static class IntegerCache {
    static final int low = -128;
    static final int high = 127;
    static final Integer cache[];  // 缓存数组
    
    static {
        // 初始化缓存：-128到127的Integer对象
        cache = new Integer[(high - low) + 1];
        int j = low;
        for(int k = 0; k < cache.length; k++)
            cache[k] = new Integer(j++);
    }
}
```

**关键点：**
- Integer缓存了 `-128 ~ 127` 的对象
- 在这个范围内，`valueOf()` 返回缓存对象
- 超出范围，`new Integer()` 创建新对象

**拆箱：`Integer.intValue()` 源码**

```java
public int intValue() {
    return value;  // 直接返回内部存储的int值
}
```

---

#### 3) 缓存机制的影响

```java
Integer a = 100;
Integer b = 100;
System.out.println(a == b);  // true（缓存对象，同一个引用）

Integer c = 200;
Integer d = 200;
System.out.println(c == d);  // false（超出缓存范围，不同对象）

// 拆箱后比较
System.out.println(c.intValue() == d.intValue());  // true
System.out.println(c.equals(d));  // true（推荐）
```

**所有包装类的缓存范围：**

| 包装类 | 缓存范围 |
|--------|----------|
| Byte | -128 ~ 127（全部） |
| Short | -128 ~ 127 |
| Integer | -128 ~ 127 |
| Long | -128 ~ 127 |
| Character | 0 ~ 127 |
| Boolean | TRUE, FALSE（两个常量） |
| Float | 无缓存 |
| Double | 无缓存 |

---

### 📝 总结

| 概念 | 过程 | 实际调用 |
|------|------|----------|
| 自动装箱 | 基本类型 → 包装类型 | `Integer.valueOf()` |
| 自动拆箱 | 包装类型 → 基本类型 | `Integer.intValue()` |

**应用场景：**
- 集合存储基本类型（`List<Integer>`）
- 方法参数传递（泛型方法）
- 算术运算（包装类型参与运算）

---

### 🔍 拓展知识：常见陷阱

#### 陷阱1：NPE空指针异常

```java
Integer obj = null;
int i = obj;  // 自动拆箱时抛出NPE

// ✅ 正确：先判空
if (obj != null) {
    int i = obj;
}
```

#### 陷阱2：性能问题

```java
// ❌ 错误：循环中频繁装箱
Integer sum = 0;
for (int i = 0; i < 1000; i++) {
    sum += i;  // 每次都要装箱拆箱
}

// ✅ 正确：使用基本类型
int sum = 0;
for (int i = 0; i < 1000; i++) {
    sum += i;
}
```

#### 陷阱3：比较问题

```java
Integer a = 100;
Integer b = 100;
System.out.println(a == b);  // true（缓存）

Integer c = 200;
Integer d = 200;
System.out.println(c == d);  // false（不同对象）

// ✅ 推荐：使用equals比较
System.out.println(c.equals(d));  // true
```

#### 陷阱4：三目运算符的空指针

```java
Integer a = null;
Integer b = 10;
// Integer result = a == null ? 0 : a;  // NPE！
// 因为三目运算符会自动拆箱a

// ✅ 正确：显式判断
Integer result = (a == null) ? 0 : a;
```

---

### ⚠️ 最佳实践

```java
// 1. 集合用包装类，计算用基本类型
List<Integer> list = new ArrayList<>();
list.add(10);       // 装箱
int num = list.get(0);  // 拆箱

// 2. 判空避免NPE
Integer obj = getInteger();
if (obj != null) {
    int value = obj;  // 安全拆箱
}

// 3. 比较用equals
Integer a = 200;
Integer b = 200;
if (a.equals(b)) {  // 推荐
    // ...
}

// 4. 性能敏感场景用基本类型
int sum = 0;  // 不用Integer
for (int i = 0; i < 10000; i++) {
    sum += i;
}
```

---

